<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mx.softia.multiplica">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Multiplica">
        <activity
            android:name=".Actividades.MenuNavegacion"
            android:exported="false"
            android:label="@string/title_activity_menu_navegacion" />
        <activity
            android:name=".Actividades.ServiciosActividad"
            android:exported="false" />
        <activity
            android:name=".Actividades.ProductosActividad"
            android:exported="false" />
        <activity
            android:name=".SesionActividad"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>